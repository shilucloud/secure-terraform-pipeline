name: Terraform CI/CD

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:
  terraform:
    runs-on: ubuntu-latest
    env:
      IS_CI: true
      TF_IN_AUTOMATION: true
      TF_CLI_ARGS: "-no-color"

    steps:
      - uses: actions/checkout@v4

      # Cache Terraform plugins & modules for speed
      - name: Cache Terraform
        uses: actions/cache@v4
        with:
          path: |
            ~/.terraform.d/plugin-cache
            .terraform
          key: terraform-${{ runner.os }}-${{ hashFiles('**/*.tf') }}

      # Install Nix (required for nix-shell)
      - name: Setup Nix
        uses: cachix/install-nix-action@v22

      # Terraform formatting check
      - name: Terraform Fmt
        run: nix-shell shell.nix --pure --run "terraform fmt -check -recursive"

      # Init Terraform
      - name: Terraform Init
        run: nix-shell shell.nix --pure --run "terraform init -backend-config='bucket=my-tfstate-bucket' -backend-config='key=global/terraform.tfstate'"

      # Validate Terraform configs
      - name: Terraform Validate
        run: nix-shell shell.nix --pure --run "terraform validate"

      # Security scanning (Checkov)
      - name: Run Checkov
        run: nix-shell shell.nix --pure --run "checkov -d ./terraform -o json > checkov-report.json"

      # Policy checks (Conftest)
      - name: Conftest Policy Check
        run: nix-shell shell.nix --pure --run "terraform show -json | conftest test --output=json > conftest-report.json"

      # Terratest
      - name: Run Terratest
        run: nix-shell shell.nix --pure --run "go test ./test -v -timeout 30m | tee terratest-results.txt"

      # Terraform Plan
      - name: Terraform Plan
        run: nix-shell shell.nix --pure --run "terraform plan -out=tfplan | tee terraform-plan.txt"

      # Upload reports
      - name: Upload Reports
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            checkov-report.json
            conftest-report.json
            terratest-results.txt
            terraform-plan.txt
            tfplan

      # PR comment
      - name: PR Comment with Results
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: terraform-summary
          message: |
            ## ✅ Terraform Validation Summary

            - **Terraform Fmt/Validate:** Passed  
            - **Checkov Security Scan:** See artifact (`checkov-report.json`)  
            - **OPA/Conftest Policies:** See artifact (`conftest-report.json`)  
            - **Terratest Results:** See artifact (`terratest-results.txt`)  
            - **Terraform Plan:** Uploaded (`terraform-plan.txt`)  

            :warning: Please review reports before merging.

  apply:
    needs: terraform
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://mycompany-infra.example
    permissions:
      contents: read
      id-token: write
    env:
      IS_CI: true
      TF_IN_AUTOMATION: true
      TF_CLI_ARGS: "-no-color"

    steps:
      - uses: actions/checkout@v4

      - name: Download plan and reports
        uses: actions/download-artifact@v4
        with:
          name: reports
          path: ./reports

      - name: Setup Nix
        uses: cachix/install-nix-action@v22

      - name: Terraform Init
        run: nix-shell shell.nix --pure --run "terraform init -backend-config='bucket=my-tfstate-bucket' -backend-config='key=global/terraform.tfstate'"

      - name: Terraform Apply
        run: nix-shell shell.nix --pure --run "terraform apply -auto-approve ./reports/tfplan"

      - name: Upload Apply Logs
        run: nix-shell shell.nix --pure --run "echo 'Apply finished' > apply-log.txt"
      - uses: actions/upload-artifact@v4
        with:
          name: apply-logs
          path: apply-log.txt
